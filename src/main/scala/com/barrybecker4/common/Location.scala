package com.barrybecker4.common

/**
  * Case classes automatically provide toString, and equals/hashcode implementations
  * @param row row coordinate
  * @param col column coordinate
  */
case class Location(row: Int, col: Int) {
}
